---
title: Your Title Here
devanagari: देवनागरी शीर्षक यहाँ
date: "2026-04-09"
excerpt: A one or two sentence summary used in listings and at the top of the entry page.
form: PROSE
---

Your opening paragraph goes here. The first paragraph gets special folio styling on the site, so this is a good place for a strong opening rather than a repeated excerpt.

## Section Heading

Use regular paragraphs for the main body. Keep the prose clean and readable, with blank lines between paragraphs.

### Smaller Heading

You can use **bold text**, *italic text*, and `inline code` where useful.

- Use bullet lists for short grouped points.
- Keep each item concise.
- Prefer simple markdown over heavy formatting.

1. Use numbered lists when order matters.
2. Keep the sequence easy to follow.
3. Avoid nesting unless it is truly necessary.

> Use blockquotes for quotations, notes, or emphasis.
> They already have custom styling in the folio layout.

[Links are supported](https://example.com) and render as standard anchors.

***

## Code Block Example

```text
Plain fenced code blocks are supported.
Use them for code, commands, or literal examples.
```

## Minimal Entry Example

Write a few closing paragraphs here if the piece needs them. Remove any sections from this template that are not relevant to the page you are creating.

## Notes

- `title`, `date`, and `excerpt` should always be present.
- `devanagari` is optional in code, but most existing entries include it.
- `form` is optional and currently appears on some catalog cards.
- Use `YYYY-MM-DD` for `date`.
- This setup supports standard markdown via `remark`, not GitHub-flavored markdown tables or task lists.
